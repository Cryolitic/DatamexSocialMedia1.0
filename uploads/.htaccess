# Protect uploads directory
# Allow only image files to be accessed directly
<FilesMatch "\.(jpg|jpeg|png|gif)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

# Deny access to other file types
<FilesMatch "\.(php|html|js|css|txt)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>
